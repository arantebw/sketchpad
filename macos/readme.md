# MacOS

Resize the terminal font sizes.

```text
command + shift + `+`
command + `-`
command + 0
```

Really quit the current open app.

```text
command + q
```

Fill the screen size of an open app.

```text
fn + control + f
```

Lock the screen.

```text
control + command + q
```
